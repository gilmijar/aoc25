[project]
name = "aoc25"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bitarray>=3.8.0",
    "numba>=0.62.1",
]

[tool.uv.workspace]
members = [
    "day1",
]
